# Additional inline reference screenshots

The six original file attachments are in references/images. Later screenshots were visible in the conversation but no separate original backing files were exposed in this runtime; their observations are preserved here rather than inventing image download files.

OpenCode settings screenshots: grouped left navigation; Providers screen with provider identity, connection status and actions; Models screen with search, provider grouping and compact per-model controls. Use the interaction grammar, not the product branding, provider availability list or model names as YCoding truth.

Later usage/statistics screenshots: a desktop page can accommodate richer dashboards, usage over time, model/provider breakdown and quota/limit summaries. Apply those patterns using real YCoding data; every unknown statistic has an explicit empty/unavailable state. See STATISTICS_AND_QUOTAS.md.

ChatGPT desktop/app references are explicitly allowed by the user for layout quality. The original attached app-shell, sidebar, composer and settings images capture the relevant spacing/interaction targets. Do not copy assets, logos, telemetry behavior or unrelated voice/account features. Interface text uses system fonts; no font binaries are distributed in this kit.

Reference precedence: latest explicit requirements > agreed YCoding design > actual TUI capability > screenshot grammar > current desktop implementation. The latest requirements add multi-project/folders, a walkable user avatar, all relevant settings, multipage rich content, statistics/quota, build/install and GitHub Releases/Pages. They supersede earlier prototype restrictions, but not runtime ownership or truthfulness.
