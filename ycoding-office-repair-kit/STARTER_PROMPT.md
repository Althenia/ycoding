# Copy this entire prompt into the local coding agent

Continue YCoding Desktop Office repair and completion in `/Users/viadz/Workspace/Project/ycoding` (verify this path first). The companion directory `ycoding-office-repair-kit` contains the request, latest addendum, visual references, scoped design, catalog, task/evidence ledgers and guarded delivery tooling. Locate it beside the checkout or use the path supplied by me.

You own implementation, not merely an audit. Read current root and apps/office/AGENTS.md, git status/diff, and existing plans before edits. Preserve all unrelated uncommitted work. Never stage, commit, reset, clean, rebase or switch branch unless I explicitly authorize it. Do not overwrite repository AGENTS.md with the kit's guide.

Read README, references/REQUEST.md, references/ADDENDUM.md, docs/REMOTE_AUDIT.md, MILESTONES, PROJECT_LAYOUT, docs/UI_SPEC.md, docs/MULTIPAGE_NAVIGATION.md, docs/MULTI_PROJECT.md, docs/PLAYER_CONTROLS.md, docs/ALL_SETTINGS.md and docs/STATISTICS_AND_QUOTAS.md. Inspect the six reference images and INLINE_REFERENCES notes. Audit current TUI behavior, configuration/Schema/Protocol, provider auth, settings, usage and actual native app. Earlier “complete” claims, concept videos and synthetic flow tests are not evidence of finished production behavior.

Approved direction: native Godot client using existing local YCoding HTTP/SSE; no Electron rewrite, new orchestration engine or direct desktop database access. Ordinary user prompts remain unchanged. Persistent left app sidebar; expansive office content; bottom-centered multiline composer; source-linked conversations; rich Sessions/Settings/Statistics pages. Real provider startup and actionable errors, never silent demo fallback. All relevant YCoding settings (including advanced/not mentioned) must have wired scope-aware controls or a documented truthful treatment; unknown support remains an audit task. Add independent projects/folders and a walkable user avatar with focus-safe controls. Runtime authority and human approval never depend on avatar positions or animations.

Start R0 with actual native and TUI baseline, read-only optional tools/local_audit.py output, wire/config inventory, visual and provider reproduction. Reconcile current valid work instead of rebuilding it. Continue through fixes and targeted verification into R1 and the dependency-ready tasks. Update incompatible old floating-layout/demo tests/docs with the product change rather than weakening tests. Make focused failing tests for verified defects, fix root causes, run affected/neighboring suites, launch the native app, compare screenshots and iterate. Keep a complete field/action coverage ledger; settings coverage is not proof of behavior. Never use fake credentials, canned provider output or guessed quota balances.

For releases, inspect the already-existing build-release.sh, install.sh --office, release.yml and pages.yml. Preview tools/merge_delivery.py; it uses pinned hashes and refuses drift. Merge manually with tests if the current source differs; never force overwrite. Run actual Godot/native tests before export, preserve artifact names/CLI compatibility, protect publishing with office-release environment review, and extend existing Pages at /ycoding/office/. No push/tag/publish/deploy until explicitly authorized. Signing/real-provider/platform requirements remain blocked if their actual external prerequisites are missing.

Keep tracking/tasks.json authoritative, record evidence in tracking/evidence.json, regenerate TRACKING/TODO and update HANDOFF each session. All statuses in this kit are initially unverified. Completion requires actual code + native UI + real provider + parity + supported install/release evidence. Report current state, decision, next milestone, main risk and exact verification. Continue after audit; do not stop at a list of suggestions.
