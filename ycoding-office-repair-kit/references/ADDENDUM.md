# Latest approved requirements — continuation addendum

Repository confirmed: https://github.com/Althenia/ycoding . Local path requested: /Users/viadz/Workspace/Project/ycoding . Local path is an input to verify, not evidence of current access.

The output requested is a ZIP usable for local development. Include the audit/repair/MVP/workplan/milestones/layout/AGENTS/tracking/todo/checklist/starter prompt, all relevant references, practical build/install task scripts, GitHub Release pipeline and the existing GitHub website deployment.

Added requirements: multiple projects and folders with independent sessions; user can walk inside the office; settings use the supplied OpenCode screens and allowed ChatGPT desktop interaction patterns; the desktop supports multiple pages and rich content; add a statistics screen inspired by junhoyeo/tokscale and better per-provider quota/limits; include all settings the user did not explicitly enumerate by auditing current YCoding sources. No production mock/demo substitution. Keep Godot and the existing HTTP/SSE service. Normal prompting remains sufficient.

The most recent requests supersede the earlier prototype's fixed single-project scope, four-state office, floating rail, missing stats/settings and demo startup. Keep the separation between runtime truth and cosmetic animation. This ZIP is a repair/development package, not a claim that those product changes have already been executed.
