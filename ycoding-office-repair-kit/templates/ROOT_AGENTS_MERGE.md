# Narrow repository instruction reconciliation

Read the existing root/package guide first. Do not replace it with this kit's guide. If current instructions still say TUI-only, the user's explicit new requirement authorizes work on the existing Godot desktop surface, not removal of every other boundary.

Proposed small addition, adapted to actual naming:

> The user-approved Godot Desktop Office is an additional presentation client of the existing YCoding service. Preserve the TUI, canonical runtime/configuration ownership, current permission/guardrail rules and generated-contract discipline. Office animation never executes provider work or owns durable session state. Production desktop startup must not silently use demo/fixture responses. Desktop completion requires real provider-backed UI evidence and native visual review in addition to appropriate tests.

Update product/architecture/workspace policy only where an actual conflicting rule remains. Do not create an unnecessary package.json for a non-JavaScript project just to fit a historical workspace plan. Preserve all unrelated instructions and document the diff.
