# YCoding v0.6.8

## Added

- Add Runpod Serverless vLLM and Ollama Jobs endpoints from `/connect`, with separate endpoint model entries and named API-key profiles.

## Fixed

- Make endpoint dialog actions and choices readable in dark and light themes, focus fields with the mouse, and keep long provider suggestions inside the dialog.
- Report vLLM cached prompt tokens when the worker supplies them without treating missing cache usage as zero.
