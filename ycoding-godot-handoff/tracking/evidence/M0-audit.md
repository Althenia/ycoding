# M0 — repository and toolchain audit evidence

Task: TASK-001–008 · Date: 2026-09-15

## Checkout

| Fact | Value | Command |
|---|---|---|
| Repository root | `/Users/viadz/Workspace/Project/ycoding` | `git rev-parse --show-toplevel` |
| HEAD | `8544ea9fa55e0c86dcc09ac7d4b43dc7ee6dba10` | `git rev-parse HEAD` |
| Branch | `main` | `git branch --show-current` |
| Remote | `git@github.com:Althenia/ycoding.git` | `git remote -v` |
| Working tree at audit | clean except untracked `ycoding-godot-handoff/` | `git status --short` |

The local HEAD equals the commit pinned by `docs/REPO_AUDIT.md`, so the remote
snapshot does describe this checkout. No Git mutation was performed.

## Toolchain

| Tool | Version | Path |
|---|---|---|
| Godot | `4.7.2.stable.official.ed1daf0bf` | `/Applications/Godot.app/Contents/MacOS/Godot` (same binary as `/opt/homebrew/bin/godot`) |
| Python | 3.14.7 | `python3` |
| Bun | 1.4.2 (declared `bun@1.4.2`) | mise shim |
| Node | v24.21.0 | `node` |
| podman | 6.0.2 | `/opt/podman/bin/podman` |
| ffmpeg | **absent** | — |

Renderer at runtime: `OpenGL API 4.1 Metal — Compatibility`.

### Blockers recorded

- **Godot 4.7.2 export templates are NOT installed.** `~/Library/Application Support/Godot/export_templates/` exists and is empty; `~/.local/share/godot/` is absent. TASK-044 cannot pass until templates are installed; that is an environment change requiring user approval.
- `ffmpeg` is absent. This only blocks optional MP4 conversion, not Godot capture.

## Engine flags verified present in 4.7.2 `--help`

`--headless`, `--import`, `--script`, `--write-movie`, `--fixed-fps`,
`--quit-after`, `--export-release`, `--resolution`, `--print-fps`, and the
`--`/`++` user-argument separator. `--write-movie` forces `--fixed-fps`, which
disables real-time sync, so a movie capture is not performance evidence.

## Pack utility checks (executed)

| Command | Result |
|---|---|
| `python3 -B tools/validate_pack.py` | PASS — 48 tasks, 6 milestones, 2 fixtures / 53 observations |
| `python3 -B tools/test_tools.py` | PASS — 14 tests |
| `python3 -B tools/render_tracking.py --check` | PASS — generated views current |
| `python3 -B tools/doctor.py --repo <checkout>` | PASS — read-only inventory |

## Baseline repository checks

| Command | Result |
|---|---|
| `bun test` (root) | exits 1 by design — `package.json` prints "do not run tests from root" |
| `bun run check:ycoding-workspace` | PASS — 17 packages |
| `cd script && bun test ycoding-workspace.test.ts ycoding-package-identity.test.ts` | PASS — 4 tests |
| `cd packages/core && bun run typecheck` | PASS |
| `./node_modules/.bin/oxlint packages/core/src/plugin/agent.ts packages/core/test/agent.test.ts` | exit 0, 2 pre-existing warnings at agent.test.ts:21-23 |
| `bun run lint:effect-patterns` | PASS |

`bun run lint` over the whole repository exceeded a 3 GiB memory cap before
completing; the scoped lint above was used instead and is recorded as a partial
substitute for the repository-wide lint.

## Wire contract

`contracts/wire-audit.json` was replaced with locally verified operations. Key
corrections against the original pack:

- `GET /api/experimental/session/:sessionID/log?after=<exclusive Event.Seq>&follow=<bool>` **does** replay durable session events and emits `log.synced`. Executable proof: `packages/core/test/session-log.test.ts`, 7 pass / 0 fail.
- Global `GET /api/event` remains volatile: frames are exactly `data: <json>\n\n` with no `id:`/`retry:` field and no `Last-Event-ID` handling. Proof: `packages/server/test/event-feed.test.ts`, 5 pass / 0 fail.
- The real event vocabulary is `session.*` names (`session.execution.*`, `session.task.updated`, `session.input.*`, `session.step.*`, `session.tool.*`, `session.status`). The five fixture labels are client-internal and absent from all live code.

## Boundary and asset decisions

- Native project placed at `apps/office/`, outside `workspaces.packages` and outside the `packages/`-only scan of `script/ycoding-workspace.ts`. Precedent: the tracked non-package surface `extensions/chrome`.
- Asset strategy: original in-house pixel-art family generated by `apps/office/tools/generate_art.py`. No third-party pack, so no license or redistribution constraint. See `tracking/asset_manifest.json`.
