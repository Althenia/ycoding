# Demo fixtures

Synthetic DEMO-only input, copied from the approved handoff pack. These are
client-internal observations, not captured server traffic. `FixtureTranslator`
converts them to the verified wire vocabulary before they reach `OfficeStore`.
