// OpenRouter Responses is the canonical OpenRouter implementation.
// This entrypoint re-exports the primary provider so
// `@ycoding-ai/ai/providers/openrouter-responses` remains a valid specifier
// without duplicating protocol logic.
export * from "./openrouter"
